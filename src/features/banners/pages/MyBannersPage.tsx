import { Typography, Box } from "@mui/material";

const MyBannersPage = () => {
  return (
    <Box>
      <Typography variant="h2"> Banner Shop </Typography>
      <Typography variant="h5"> My Banners </Typography>
      {/* <Link to={'/SignUp'} component={<SignUpPage/>} >
          {Children}
        </Link> */}
    </Box>
  );
};

export default MyBannersPage;
